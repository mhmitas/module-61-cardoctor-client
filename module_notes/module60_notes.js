/*

# module 60:

60_2: Auth Redirect and JWT core concepts.
```````````````````````````````````````````
- i can set restriction in client side to prevent user to access data if he is not log in.
but id someone try he can easyly access data from server. 
 - to solve this problem I need JWT core concept.

- ? what is JWT ?
- ? why do we use JWT ?


#60_3: Access Token Refresh Token And Create Simple Access Token
``````````````````````````````````````````````````````
// typically ekhane 2 types token thake 
-> access token -> refresh token (access token expire hoye gele eti diye token generate kore kaj kora jabe)

// `token` = 'user login korbe' tokhn server er kache req jabe token er jonno.
- server 'token' generate kore client er kache send korbe. then client side e seta kothao ekta store kore rakha hobe.
- first jokhn token create kora hoy tokhon 2 type er token create kora hoy.














*/
